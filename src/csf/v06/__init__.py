"""
CSF v0.6 — Convergence-Fitted Searchable Binary Archive
Classical Optimized, Quantum Dust Aware

Modules:
  qutrit_delta      — Qutrit state and delta packing (amplitude + phase)
  quantum_dust      — Default free-state management for 3^12 matrix
  convergence_engine — Background convergence pass with clustering
  classical_compressor — Hybrid pipeline: dict + sparse + delta + zstd
  csf_file          — Binary format writer/reader for v0.6
"""

__version__ = (0, 6, 0)
