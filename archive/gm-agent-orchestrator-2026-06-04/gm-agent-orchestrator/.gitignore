# local/private config
config/agents.json
config/projects.json
config/agent-limits.json

# legal / personal — never commit, even in private repo
docs/legal/
private/

# runtime state
logs/*
tasks/queue/*
tasks/active/*
tasks/done/*
tasks/failed/*
tasks/hold/*
status/*-main.json
*.lock

# Agent skill scaffolding directories (prevent accidental recreation)
.agents/

# dependencies
node_modules/

# GPT Web API runtime artifacts
tools/gpt-web-api/.sessions/

# keep folder placeholders
!logs/.gitkeep
!tasks/queue/.gitkeep
!tasks/active/.gitkeep
!tasks/done/.gitkeep
!tasks/failed/.gitkeep
!tasks/hold/.gitkeep

# OS/editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/worktrees/

# Runtime status JSON
status/*.json
!status/notifications/*.json

.github-cache/
.vs/*
alex-place/gm-agent-orchestrator/
