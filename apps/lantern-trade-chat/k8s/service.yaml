apiVersion: v1
kind: Service
metadata:
  name: lantern-trade-chat
  namespace: lantern
  labels:
    app: lantern-trade-chat
spec:
  type: ClusterIP
  selector:
    app: lantern-trade-chat
  ports:
    - name: http
      port: 80
      targetPort: 8080
