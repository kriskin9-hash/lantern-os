# Template only — DO NOT commit real values. Create the real Secret out-of-band:
#
#   kubectl create namespace lantern
#   kubectl -n lantern create secret generic lantern-trade-chat-secrets \
#     --from-literal=GITHUB_OAUTH_CLIENT_ID=... \
#     --from-literal=GITHUB_OAUTH_CLIENT_SECRET=... \
#     --from-literal=LANTERN_SESSION_SECRET="$(openssl rand -hex 32)" \
#     --from-literal=KALSHI_API_KEY_ID=... \
#     --from-literal=KALSHI_PRIVATE_KEY="$(cat kalshi-private-key.pem)"
#
# This file documents the expected keys; values shown are placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: lantern-trade-chat-secrets
  namespace: lantern
type: Opaque
stringData:
  GITHUB_OAUTH_CLIENT_ID: "REPLACE_ME"
  GITHUB_OAUTH_CLIENT_SECRET: "REPLACE_ME"
  LANTERN_SESSION_SECRET: "REPLACE_ME_RANDOM_HEX"
  KALSHI_API_KEY_ID: "REPLACE_ME"
  KALSHI_PRIVATE_KEY: "REPLACE_ME_PEM_OR_BASE64"
