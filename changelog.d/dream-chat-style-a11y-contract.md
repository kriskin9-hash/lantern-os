- verify(a11y): font/style best-practice contract in `scripts/test-a11y.js` (the CI a11y suite). Σ₀-grounded in WCAG 2.1/2.2 (SC 1.4.3/1.4.11/1.4.4): parses the real theme tokens from `site.css` + rules from `dream-chat-ui.css` and enforces AA text-contrast in both light and dark themes, bans background/surface-intent tokens (`--accent-dim`, `--surface*`, `--border`, `--bg`) from being used as a text `color:`, and sets a 10px legibility floor. Activates the previously-dormant contrast helpers. Caught the live regression where the "Keystone · chat" thinking-wheel route card + reply signature were below AA (1.12:1 / 2.77:1); both switched to `--muted` (4.83:1). Also fixes a Test-3 false positive that flagged decorative `alt=""` (valid per WCAG H67).
