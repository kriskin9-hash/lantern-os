- docs(sigma0): refresh ADR-0011 with the current Stage-0 evidence and surface the owned-PLT track in the Knowledge Center. ADR-0011 now carries a *Progress since proposal* note (own modeling code merged #1645/#1668; 4-bit smoke **PASS** on the 3070 per `data/convergence/keystone-plt-parity-log.jsonl`; faithful logit parity still **pending** a ≥24 GB box) plus refreshed Stage-0 status + Evidence rows — the decision packet the founder gate (#1666) was waiting on. Status stays **Proposed** (only Alex flips it). `docs/SIGMA0-OURO-CODER.md` gains a *Two Σ₀-coder tracks* section disambiguating the active Ouro coder from the proposed Keystone-Σ₀ PLT owned-base (not parallel coders — one Convergence Core, evidence-gated). Added an ADR-0011 KC card to `knowledgecenter.html` and rebuilt `data/knowledge/index.jsonl` so both are searchable in product RAG. Improves the **Converge** stage (the owned-substrate decision is now evidence-grounded and discoverable). Docs only — no runtime behavior change.
