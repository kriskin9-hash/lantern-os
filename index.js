// Root package entry point. apps/lantern-garage depends on "lantern-os" (file:../..)
// to resolve the repo root via require.resolve("lantern-os") — no consumer requires
// this module's exports today.
module.exports = {};
