name: Release provenance

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  provenance-stub:
    name: Release provenance (stub)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Acknowledge
        run: echo "Release provenance workflow — no artifacts to hash in v1.0.0"
